/* ══════════════════════════════════════════════
   TALENTBRIDGE — config.js
   Set your backend URL here ONE TIME.
   This file is loaded before all other scripts.

   ┌─────────────────────────────────────────────┐
   │  HOW TO DEPLOY:                             │
   │  1. Deploy your Express server (Render,     │
   │     Railway, Fly.io etc.)                   │
   │  2. Copy the deployed URL                   │
   │  3. Replace the URL below                   │
   │  4. Re-deploy or update the HTML files      │
   └─────────────────────────────────────────────┘
══════════════════════════════════════════════ */

// ✅ Change this to your deployed backend URL before going live.
//    Keep it as http://localhost:5000 for local development.
window.TB_API_BASE = "http://localhost:5000/api";
