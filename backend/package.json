{
  "name": "talentbridge-backend",
  "version": "1.0.0",
  "description": "TalentBridge API server (Express + TiDB) — deployed on Railway",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors":    "^2.8.6",
    "dotenv":  "^16.4.5",
    "express": "^4.18.2",
    "mysql2":  "^3.9.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
